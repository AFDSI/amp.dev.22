
Carlos Vializ

AMP HTML Thread
  https://groups.google.com/forum/#!topic/amphtml-discuss/8TjRgR2VzjI

Reference:
  https://afdsi.org/test/amp_autosuggest/
using
  src="https://ampbyexample.com/advanced/autosuggest/search_list"
  [src]="'https://ampbyexample.com/advanced/autosuggest/search_list?q=' + (autosuggest ? autosuggest : '')"

Two questions?

1  why does google data work and afdsi data fail?
  google success:
    src="https://ampbyexample.com/advanced/autosuggest/search_list"
    [src]="'https://ampbyexample.com/advanced/autosuggest/search_list?q=' + (autosuggest ? autosuggest : '')"
  afdsi fail:
    src="https://afdsi.org/test/amp_autosuggest/query"
    [src]="'https://afdsi.org/test/amp_autosuggest/query?q=' + (autosuggest ? autosuggest : '')"
  afdsi failed JSON:
    https://afdsi.org/test/amp_autosuggest/query.json
  afdsi is HTTPS and CORS enabled

do I need to install an endpoint? do I need to install:
  https://github.com/ampproject/amp-by-example/blob/master/backend/autosuggest.go

2  Once the autosuggest with adfsi-data works, do the following HTML and JSON changes correctly implement your advice?

replace existing:
      <template type="amp-mustache">
        <p>Here are the results for the search "{{query}}":</p>
        <ul>
            {{#results}}<li>{{title}}</li>{{/results}}
        </ul>
      </template>

with new:

      <template type="amp-mustache">
        <p>Here are the results for the search "{{query}}":</p>
        <ul>
            {{#results}}<li><a href="#{{id}}">{{title}}</a></li>{{/results}}
        </ul>
      </template>

and modify JSON to include "title" and "id":

{
"items":
  [{
    "query": "",
    "results": [
      {"title": "Annapolis, Maryland", "id":"1"},
      {"title": "Boise, Idaho", "id":"2"},
      {"title": "Richmond, Virginia", "id":"3"}
    ]
  }]
}